import {cases} from './mockData';

// BACKEND
cases = JSON.stringify();

export const apiGetCases = () => {
    
    return new Promise;
}

export const apiGetCase = () => {

}


// FRONTEND
apiGetCases(params, query, body).then( (cases) => {

} );

axios.get('/cases/:id', body).then( (cases) => {

} )